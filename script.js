 // Ação dos botôes

    function contact() {
        // seleciona o elemento que deseja modificar
        var div = document.querySelector(".form");
        
        //localiza e mostra através do ip
        window.location = "#form";

        // modifica o display para "block" fazendo com que o formulário apareça          
        div.style.display = "block";

    } ;
    
    function aboutMe() {
        
        // seleciona o elemento que deseja modificar
        window.location = "#text-aboutme";

    } ;

    function education() {
        
        // seleciona o elemento que deseja modificar
        window.location = "#text-education";

    } ;
    
    function skills() {
        
        // seleciona o elemento que deseja modificar
        window.location = "#text-skills";

    } ;  

    function portifolio() {
        
        // seleciona o elemento que deseja modificar
        window.location = "#text-portifolio";

    } ;